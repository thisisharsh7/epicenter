/**
 * Markdown index subpath exports
 *
 * Export markdown-specific utilities for custom serializers and error handling.
 * Only needed when implementing custom markdown serializers/deserializers.
 */

export { MarkdownIndexErr, MarkdownIndexError } from './index';
export type { MarkdownIndexConfig } from './index';
