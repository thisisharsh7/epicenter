import Root, {
	type ButtonProps,
	type ButtonPropsWithoutHTML,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from './button.svelte';

export {
	//
	Root as Button,
	type ButtonProps,
	type ButtonPropsWithoutHTML,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
	type ButtonProps as Props,
	Root,
};
